# Always use Unix (LF) line endings for shell scripts
*.sh text eol=lf

# Normalize SQL files too (optional but good)
*.sql text eol=lf

# Normalize YAML and Go files
*.yml text eol=lf
*.yaml text eol=lf
*.go text eol=lf
